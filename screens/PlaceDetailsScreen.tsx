import { StyleSheet, Text } from "react-native";

const PlaceDetailesScreen = () => {
  return <Text>Place Detailes Screen</Text>;
};

export default PlaceDetailesScreen;

const styles = StyleSheet.create({});
